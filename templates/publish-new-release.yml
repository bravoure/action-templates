name: "Publish new release"

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  release:
    uses: bravoure/action-templates/.github/workflows/v2/publish-new-release.yml@main
