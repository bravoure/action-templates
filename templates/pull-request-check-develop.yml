name: Build, Run, Test, Lint and Succeed.

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches:
      - develop
    types:
      - ready_for_review

jobs:
  check:
    uses: bravoure/action-templates/.github/workflows/pull-request-check.yml@main
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
